/**
 * Feature Flags
 * Centralized configuration for enabling/disabling features.
 */

export const FLAGS = {
    /** When true, hides add/delete/move widget controls. Only text content remains editable. */
    WIDGETS_LOCKED: true,
} as const;
